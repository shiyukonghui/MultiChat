以下是为你整理编写的《多模型同时对话系统需求规格说明书》。它遵循了常见的 SRS 结构，并已针对你选定的技术栈和交互模式做了具体描述，可直接作为开发基准。

---

## 1. 引言

### 1.1 编写目的
本文档旨在清晰定义 **MultiChat**（多模型并发对话系统）的功能需求、性能要求、系统架构及接口规范，为项目的设计、开发、测试和验收提供统一依据。

### 1.2 项目范围
构建一个 Web 应用，允许用户在单一界面输入一次提问，系统**并发发送至多个已配置的大语言模型（LLM）**，并通过**流式响应（SSE）实时推送**各模型回复。用户可通过**侧边栏**自由切换、对比不同模型的完整回答，支持**多轮对话**保留上下文，并提供**前端模型配置管理界面**。

### 1.3 技术选型
- **后端**：Rust (llmg-gateway)
- **前端**：React + TypeScript + Material-UI (MUI)
- **通信协议**：Server-Sent Events (SSE) 实现流式响应

### 1.4 用户群体
- 需要横向比较多个 LLM 回答质量的开发者、研究人员
- 对复杂问题需要多角度参考的普通用户

---

## 2. 总体描述

### 2.1 产品视角
MultiChat 是一个单页应用（SPA），由前端 React 应用和后端 Rust 服务组成。后端通过统一网关（llmg-gateway）屏蔽不同 LLM Provider 的 API 差异，向上层提供统一的聊天接口。前端通过 SSE 接收各模型的流式回复，实现实时渲染。

### 2.2 工作流概览
1. 用户在输入框键入问题，点击发送。
2. 前端向后端 `GET /api/chat/stream` 发起 SSE 连接，携带消息和对话历史。
3. 后端内部**并发**调用所有已启用的模型。
4. 每个模型返回内容时，后端通过 SSE **实时推送**到前端。
5. 前端收到首个模型的回复后立即展示，**最快返回优先**。
6. 侧边栏实时更新各模型状态（加载中/已完成/失败）。
7. 用户点击侧边栏中的模型名，右侧内容区立即切换至对应回复。

### 2.3 用户特征
用户应具备基本的浏览器操作能力，无需任何编程知识即可使用对比功能和模型配置管理。

### 2.4 假设与依赖
- 所有待调用的 LLM 均提供稳定的 HTTP API。
- 系统部署环境可访问外部 LLM Provider 的网络。
- 各模型所需的 API Key 已通过环境变量正确注入后端容器。
- 前端与后端处于同一可信任网络（初期阶段），若部署至公网需增加认证。

---

## 3. 功能需求

### 3.1 用例图与角色
**角色**：普通用户（User）

**用例列表**：
- UC1：发送问题并获取多模型流式回复
- UC2：在侧边栏切换查看不同模型的完整回答
- UC3：多轮对话（携带对话历史）
- UC4：模型配置管理（前端管理界面）

### 3.2 具体功能描述

#### UC1：发送问题并获取多模型流式回复
**输入**：一段自然语言文本（不超过 4000 字符）。
**处理流程**：
1. 前端校验输入非空且不超过 4000 字符。
2. 发起 GET 请求至 `/api/chat/stream`，通过查询参数传递消息和对话历史。
3. 后端收到请求后，从配置中读取所有已启用的模型列表。
4. 使用 `llmg-gateway` 统一接口，对每个模型生成一个异步任务，**同时**发送相同的消息。
5. 每个模型产生内容时，后端通过 SSE **实时推送**增量内容到前端：
   - 事件类型 `chunk`：模型的部分回复内容
   - 事件类型 `done`：模型回复完成
   - 事件类型 `error`：模型调用失败
6. 前端收到 SSE 事件后：
   - 实时更新对应模型的回复内容。
   - **最快返回优先**：首个返回内容的模型自动成为当前选中模型，右侧主区域立即展示其流式回复。
   - 侧边栏实时更新各模型状态（加载中/已完成/失败），当前选中项高亮。

**异常情况**：
- 某个模型调用失败：通过 SSE 推送 `error` 事件，前端在该模型区域显示用户友好的错误提示（如"模型响应超时，请稍后重试"），不影响其他模型正常展示。
- 所有模型均失败：前端显示"所有模型暂不可用"。
- SSE 连接中断：前端自动重连，并提示用户"连接已断开，正在重连..."。

#### UC2：侧边栏切换模型回复
**前置条件**：当前存在至少一条多模型对话结果。
1. 用户在左侧侧边栏点击某个模型名称。
2. `selectedModel` 状态更新为该模型名。
3. 右侧内容区立即替换为 `responses` 中对应模型的 `content`，平滑过渡。

**视觉要求**：
- 侧边栏模型列表项应可点击，当前选中项有明显视觉标识（如背景色、左侧边框加粗）。
- 侧边栏各模型项应显示实时状态图标：加载中（旋转动画）、已完成（绿色勾选）、失败（红色感叹号）。
- 右侧面板在切换时无页面刷新，为异步更新。

#### UC3：多轮对话
**前置条件**：当前会话中已有至少一轮对话。
1. 用户在输入框继续提问。
2. 前端将当前问题与完整对话历史（`messages` 数组）一同发送至后端。
3. 后端将对话历史转发给各模型，保留上下文。
4. 各模型基于上下文生成回复，通过 SSE 流式返回。

**对话历史管理**：
- 对话历史存储在前端 `localStorage` 中，页面刷新后保留。
- 每轮对话记录包含：用户消息、各模型的回复。
- 对话历史以会话为单位管理，用户可创建新会话或清空当前会话。
- Token 限制策略：保留最近 10 轮对话，超出时自动丢弃最早的记录。

#### UC4：模型配置管理（前端管理界面）
**前置条件**：用户已进入模型配置页面。
1. 前端通过 `GET /api/models` 获取当前所有可用模型列表及状态。
2. 用户可在界面中启用/禁用模型，修改后实时生效，无需重启服务。
3. 用户可查看每个模型的基本信息（名称、Provider、状态）。

**配置界面要求**：
- 以列表形式展示所有模型，每项包含：模型名称、Provider、启用/禁用开关、当前状态。
- 禁用的模型不参与后续对话请求。
- 配置变更通过 `PUT /api/models/{id}` 接口提交到后端。

---

## 4. 非功能需求

### 4.1 性能需求
- **并发请求**：后端向各模型发起的请求必须**并发执行**，总响应时间应接近耗时最长的单个模型响应时间。
- **流式首字节**：首个模型的流式内容应在 2 秒内开始推送到前端。
- **超时控制**：单模型请求超时时间 60 秒，超时后返回超时错误信息，不阻塞其他模型。
- **资源占用**：在 10 个模型并发时，后端内存占用应小于 200MB。

### 4.2 可靠性
- 单个模型的调用失败不应导致整个请求返回 5xx 错误。
- 网关应提供基本的重试机制（最多重试 1 次，间隔 1 秒），仅对网络错误和 5xx/429 错误重试，4xx 客户端错误不重试。
- 重试时间计入总超时时间。

### 4.3 可维护性
- 后端模型配置应集中存放于 `config/models.yaml`，作为初始默认配置。
- 前端管理界面的配置变更实时生效，无需重启服务。
- 前端 UI 组件应模块化，未来增加新功能（如暗黑模式）时改动最小。

### 4.4 安全性
- API Key 等敏感信息**仅**通过环境变量注入，禁止硬编码。
- 环境变量命名规范：`LLM_API_KEY_{PROVIDER}`（如 `LLM_API_KEY_OPENAI`、`LLM_API_KEY_ANTHROPIC`）。
- 日志中禁止记录完整 API Key，仅显示前 4 位 + 掩码。
- 后端 API 应限制请求体大小（例如 10KB），防止资源滥用。
- 若部署至公网，必须增加 API 认证（如 API Key 验证）和 HTTPS。
- 后端架构应预留认证接口设计（`Authorization: Bearer <token>`），V1.0 可不强制启用。

### 4.5 可用性
- 系统应提供用户友好的错误提示：网络异常、服务器内部错误、模型超时等，区分不同错误类型。
- UI 需符合 WCAG 2.1 AA 级可访问性标准（MUI 组件默认支持良好）。
- 前端发送按钮在请求期间禁用，防止重复提交。

---

## 5. 系统架构与组件

### 5.1 架构图

```
┌─────────────────────────────────────┐
│             用户浏览器                 │
│  ┌──────────────────────────────┐   │
│  │   React SPA (MUI)            │   │
│  │   - 输入组件                  │   │
│  │   - 侧边栏 (模型列表+状态)    │   │
│  │   - 主显示区 (流式回复)       │   │
│  │   - 模型配置面板              │   │
│  └─────────┬────────────────────┘   │
└────────────┼────────────────────────┘
             │ SSE + REST
┌────────────▼────────────────────────┐
│          Rust 后端服务 (Axum)        │
│  ┌──────────────────────────────┐   │
│  │  /api/chat/stream (SSE)      │   │
│  │  - 并发任务协调                │   │
│  │  - 流式结果推送                │   │
│  └─────────┬────────────────────┘   │
│  ┌──────────────────────────────┐   │
│  │  /api/models (REST)          │   │
│  │  - 模型列表查询               │   │
│  │  - 模型配置更新               │   │
│  └─────────┬────────────────────┘   │
│            │                         │
│  ┌─────────▼────────────────────┐   │
│  │    llmg-gateway (Gateway)     │   │
│  │  - 统一模型调用接口            │   │
│  │  - 误差处理/重试              │   │
│  └─────────┬────────────────────┘   │
└────────────┼────────────────────────┘
             │
    ┌────────┼────────┬──────────┐
    ▼        ▼        ▼          ▼
  模型A    模型B    模型C      模型...
 (OpenAI) (Anthropic) (Google)
```

### 5.2 技术栈细节
- **后端**：
  - Web 框架：Axum
  - LLM 网关：`llmg-gateway` (v0.3+)，内置 70+ Provider 支持
  - 异步运行时：Tokio
- **前端**：
  - 框架：React 18 + TypeScript
  - UI 库：MUI v5
  - HTTP 客户端：Axios（REST 请求）+ EventSource API（SSE 连接）
  - 状态管理：React Hooks (useState/useReducer)，无需引入 Redux

---

## 6. 接口设计

### 6.1 后端 API

#### **GET /api/chat/stream**（SSE 流式对话）
- **描述**：向所有已启用的模型发送同一消息，通过 SSE 实时推送各模型回复。
- **请求参数**（查询参数）：
  - `message`：用户消息（必填，不超过 4000 字符）
  - `history`：JSON 编码的对话历史数组（可选，多轮对话时传入）
- **SSE 事件格式**：

  **chunk 事件**（模型增量内容）：
  ```
  event: chunk
  data: {"model": "gpt-4o", "content": "Rust 的异步机制"}
  ```

  **done 事件**（模型回复完成）：
  ```
  event: done
  data: {"model": "gpt-4o", "content": "Rust 的异步机制基于 Futures 和 Tokio..."}
  ```

  **error 事件**（模型调用失败）：
  ```
  event: error
  data: {"model": "claude-3-opus", "error": {"code": "TIMEOUT", "userMessage": "模型响应超时，请稍后重试"}}
  ```

- **错误响应**（HTTP 层，连接建立前）：
  ```json
  {
    "error": "Invalid request: message is required"
  }
  ```

#### **GET /api/models**（获取模型列表）
- **描述**：获取所有可用模型列表及当前状态。
- **成功响应** (HTTP 200)：
  ```json
  [
    {
      "id": "gpt-4o",
      "name": "GPT-4o",
      "provider": "OpenAI",
      "enabled": true,
      "status": "available"
    },
    {
      "id": "claude-3-opus",
      "name": "Claude 3 Opus",
      "provider": "Anthropic",
      "enabled": true,
      "status": "unavailable",
      "reason": "API quota exceeded"
    }
  ]
  ```

#### **PUT /api/models/{id}**（更新模型配置）
- **描述**：更新指定模型的配置（启用/禁用）。
- **请求体**：
  ```json
  {
    "enabled": false
  }
  ```
- **成功响应** (HTTP 200)：
  ```json
  {
    "id": "claude-3-opus",
    "enabled": false
  }
  ```

### 6.2 前端组件接口
- `ChatInput`：接收 `onSend(message: string)` 回调，请求期间禁用发送按钮。
- `ModelSidebar`：接收 `models: ModelStatus[]`（含名称和状态）、`selectedModel: string`、`onSelect(model: string)`。
- `StreamingResponse`：接收 `modelName: string` 和 SSE 事件流，实时渲染流式回复内容，支持 Markdown 渲染和代码高亮。
- `ModelConfigPanel`：接收 `models: ModelConfig[]`，提供启用/禁用开关，调用 `onToggle(id: string, enabled: boolean)` 回调。
- `ChatHistory`：管理对话历史，支持新建会话、清空会话，从 `localStorage` 持久化读取。

---

## 7. 验收标准

| 编号 | 验收项 | 预期结果 |
|------|--------|----------|
| AC1 | 输入问题发送 | 前端发起 SSE 连接，1 秒内界面进入加载状态 |
| AC2 | 流式回复推送 | 首个模型的内容在 2 秒内开始流式显示，无需等待所有模型完成 |
| AC3 | 最快返回优先 | 首个返回内容的模型自动成为当前选中模型 |
| AC4 | 侧边栏状态 | 侧边栏实时显示各模型状态（加载中/已完成/失败），当前选中项高亮 |
| AC5 | 点击切换 | 点击侧边栏不同模型，右侧主区域内容即时切换，无页面闪烁 |
| AC6 | 单模型失败 | 失败模型显示用户友好的错误提示，其他模型正常，不报 500 |
| AC7 | 超时处理 | 模型在 60 秒内未返回则推送超时错误事件，不阻塞整体响应 |
| AC8 | 多轮对话 | 用户可连续提问，各模型基于对话历史生成回复 |
| AC9 | 对话历史持久化 | 页面刷新后对话历史保留，可继续对话 |
| AC10 | 模型配置管理 | 用户可在前端界面启用/禁用模型，变更实时生效 |
| AC11 | 模型状态查询 | 前端可查看所有模型的可用状态 |

---

## 8. 附录

### 8.1 术语表
- **LLM**：大语言模型，如 GPT-4、Claude 3 等。
- **Provider**：提供 LLM API 的服务商（OpenAI、Anthropic 等）。
- **并发**：同时向多个模型发起请求，而非依次调用。
- **SSE**：Server-Sent Events，服务端向客户端单向推送事件的协议。
- **流式响应**：模型生成内容时逐步推送，而非等待全部完成后一次性返回。

### 8.2 开发环境与工具
- 后端：Rust 1.76+, Cargo
- 前端：Node.js 20+, npm/pnpm
- IDE：VS Code + rust-analyzer + React 插件
- 版本控制：Git

### 8.3 部署建议
- 使用 Docker 将后端构建为静态二进制，配合 Nginx 前端静态文件服务。
- 模型 API Key 通过 Docker 环境变量或 Kubernetes Secret 注入。
- 若需 HTTPS，可在 Nginx 层处理。
- SSE 连接需确保 Nginx 配置关闭缓冲：`proxy_buffering off;`。

### 8.4 模型配置文件格式
后端初始配置存放于 `config/models.yaml`，格式如下：

```yaml
models:
  - id: gpt-4o
    provider: openai
    model: gpt-4o
    enabled: true
    timeout_seconds: 60
    max_tokens: 4096

  - id: claude-3-opus
    provider: anthropic
    model: claude-3-opus-20240229
    enabled: true
    timeout_seconds: 60
    max_tokens: 4096
```

API Key 通过环境变量注入，命名规范为 `LLM_API_KEY_{PROVIDER}`（如 `LLM_API_KEY_OPENAI`）。

---

## 9. 后续升级建议

### 9.1 技术栈扩展
- **langchain-ai-rust**：当需要实现链式调用、Agent、工具调用等高级功能时，可引入 `langchain-ai-rust` 框架。该框架提供了：
  - 链式 Prompt 编排
  - ReAct Agent 支持
  - 工具调用（Function Calling）抽象
  - 记忆管理和上下文压缩

### 9.2 功能演进路线
| 版本 | 功能 | 描述 |
|------|------|------|
| V1.1 | 流式响应优化 | SSE 性能优化，支持断点续传 |
| V1.2 | Agent 支持 | 引入 langchain-ai-rust，支持工具调用 |
| V1.3 | 多模态支持 | 支持图片、文件上传 |
| V1.4 | 对话持久化 | 支持用户登录，对话历史云端存储 |

---

这份文档涵盖了核心需求和设计约定，你可以直接用它指导后续的编码实现。如果在具体接口定义或组件设计上需要进一步细化，我可以继续协助。
